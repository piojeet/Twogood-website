// // JavaScript to handle the scrolling and SVG animation
// const svgElement = document.getElementById('mySvg');
// const svgRect = svgElement.getBoundingClientRect();

// window.addEventListener('scroll', () => {
//   const scrollPosition = window.scrollY + window.innerHeight;

//   // Check if the bottom of the SVG is in the viewport
//   if (scrollPosition > svgRect.top) {
//     svgElement.style.opacity = '1'; // Make the SVG visible
//   }
// });